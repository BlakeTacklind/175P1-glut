#include "FileHandler.h"
#include <iostream>
#include <fstream>
#include <string>

using namespace std;

obj* FileHandler::load(char* filename){
  /*ofstream file;
  string line;
  file.open(filename, ios::out | ios::in);
  if(file.is_open()){
    while(getline (file, line)){
      cout << line << '\n';
    }
  }
  file.close();*/
  obj* objs = new obj[1];
  obj::pnts
  obj[0] = obj(
  return 0;
}

